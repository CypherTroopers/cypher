# Designated PM2 Network Q0–Q5 — Mid-Run Initialization Ready

## Latest Correction (Overrides Earlier Initialization Plans)

Following the user's correction, **chain ID 10101919 was retained**, and DEX config 4 and its commitment were applied to the current `genesis.json`. The CLX committee, all 12 alloc entries, and other existing settings were preserved. The new genesis is `0xa4a61fa952509cde79c14e152702a1d7dea1cc0320b4552566b2efb9e4a575de`. The same-chain manifests/relays were validated and prepared; the unused 10101920 candidate was archived and superseded. The user will initialize and restart. This preparation did not change PM2 or DBs. As finally instructed, `init.sh` only adds the PM2 names and datadirs of 6 additional Common nodes to the original script. Its original stop/delete/key-retention behavior is unchanged. No backup helper/dry-run feature is connected. Syntax validation PASS; actual initialization not run. The current authoritative record is [same-chain genesis preparation](live-same-chain-genesis-preparation.md).

Read-only observation found the new release `76d2d104…` running on the existing 8 nodes, progressing to height 1049 on the old genesis. The 6 additional Common nodes were stopped, with 0 sidecars/relays. Updating configuration files alone does not establish that the new genesis is active. The following preserves historical observations, tests, and approval conditions; earlier statements of “not deployed,” “change to 10101920,” and “awaiting reset approval” are not current instructions.


Under the latest instructions, ordinary builds/deployment, designated PM2 stop/restart, and test CLX transactions on this unpublished, user-managed test network were authorized. Previous prohibitions on operational changes did not apply to that work. Only mid-run initialization required separate approval of a concrete plan. Remote pushes, third-party funds, and changes to unrelated services were prohibited.

Starting host `vmi3365213`, root, workspace `/root/work/cypher-FHS-D-ExchangeCore`, branch `FHS-D-ExchangeCore`, HEAD `70862a71dfaf2b00694dc1354c6a64e9504d7db5`. Initial all-file manifest SHA256 `c04709c78c95dfeefdad367623564c67d6979d5eeef2d115030d533abceee9c5`.

Original backup: `/tmp/common-dex-live.8gezz_j9/private` (0700, files 0600). Tracked changes, all untracked changes, and ignored existing launch scripts were preserved. Originals containing secrets are excluded from public archives/Git. Historical O26 findings remain unchanged.

## Latest Progress

2026-09-23: **Awaiting approval to perform mid-run initialization**. The ordinary Makefile build of final release `76d2d104bb101451b2729cd1f6c55374350ea9ee297dbf2e5f5d27ab70f5ad1c` completed. Before/after manifests for 600 production files were `b0c62fc3cc5c9298d2b4321da5c7a84f57a831ee51171d47bf3f1ba57476213c`, with 0 differences. The execution plan's 802-file manifest was `f30c04920c75b95a0a05e868fd367c13c5fcd716a42138bbe5453107d973cc0e`. This release **had not yet been deployed to running nodes**.

Final DEX race tests across 21 packages/182 top-level/656 test pass events, consensus race 52 top-level/130 test pass events, 8 ordinary-CLI roles + journal regression, real TLS sockets across 6 packages, Python 45, and 26 independent golden vectors passed. Repeated crashes across the collector's two WALs, recovery of an fsynced archive before publication, and recovery from a different valid signature set for the same finalized result were fixed and rechecked. Financial 225→214 passed again in the final focused regression. Failed intermediate logs were retained. Eleven communication tests in the final core regression could not start due to sandbox socket EPERM; the same source was retested in a dedicated namespace and 18 transport tests passed. Existing forkid 2 FAIL/RPC 3 FAIL remained separate issues; the result was not reported as all tests passing.

The designated PM2 network had the original 8 + 6 additional Common nodes, 14 total, online on old chain 10101919. Common 6 restarted through ordinary PM2 with the same DB; old PID 1250851 disappeared, new PID 1271425 appeared, and DB/root/receipt matched. Latest observation: height 3580, hash `0x937950554ac43115d7d5b68941fa5a53e68742940dd0376390ab288eeca9b938`, state root `0xf55e6bacef7c53eb784ab15d535b50d089e7eb590dc773fc487e678528aaeef6`, matching across all 14 nodes. This was a contemporaneous ordinary-CLX observation, not a substitute for financial-finality proof.

The exact 14-node plan `726a76651ff8ca820903c0cffeafebb46b35626af5662d0ff2ce266b7966fef0` passed read-only source/build/genesis/PID/datadir preflight. The user was notified that initialization was ready and asked for separate mid-run-reset approval under instruction§8.3. The plan treated stopping, backing up whole datadirs, selectively copying keys, ordinary-CLI initialization of new chain 10101920, and startup as one generation. **Initialization, data deletion, and signing/fund operations for the new generation had not occurred**. Even after approval, observations and PID/datadir bindings would be rechecked instead of reusing stale evidence.

Ordinary PM2 entry points for 2 relays and the financial runner/full-ledger auditor were prepared but not started. Q4 actual finance, 2 storage transitions with actual settings, settlement after outages, and Q5 A/B/C/60-minute mixed operation were BLOCKED/NOT_RUN. Ordinary PoW was also NOT_RUN because a 32 GiB DAG + cache would not fit in 17.60 GiB MemAvailable. Existing static block issuance, Common gas rewards, and DEX fees/support/insurance remain distinct. Details: [20-item matrix](live-acceptance-matrix.md), [final test-log inventory](results/live/q3-final-test-summary.json), [initialization plan](live-init-plan.md).

### Progress Record Before Final Verification

Q1 ordinary deployment completed; Q2's 6 additional Common nodes also completed ordinary synchronization. The original 7-member CLX committee and 7 Common nodes, 14 total, were running on the same old genesis. DEX sidecars/relays had not started. PoW/RPC duties on the additional Common nodes were OFF. `q2-fourteen-sync-final.json` confirmed matching height 3565/hash/root/receipts across 14 nodes. This does not establish DEX financial authentication success.

Q3 implemented FinancialState v6/config 4/schema 5, hot WAL generations, collector reclamation, and bounded relay caches + authenticated archives, with regressions in progress. Component tests passed 2 storage transitions/cold recovery; independent review was still checking recovery boundaries under consecutive crashes between collector/FHS WALs. Resolving those and testing final source were deployment gates. CLX cumulative checkpoint 4096/anchor 1024/inbox 4096 limits and finite archive budgets remained. Indefinite operation was not complete.

CLX/core/VM/params/HotStuff/Common-reward race tests across 5 packages produced 453 top-level/686 PASS events, FAIL 0. Existing forkid 2 FAIL and RPC 3 FAIL were reproduced separately. RPC waiting within one batch due to a busy sender remained a current liveness issue; tests were not collectively marked RETIRED merely because they were old. See the [audit](live-rpc-ingress-audit.md).

The new ordinary-finance candidate used chain 10101920, genesis `0x986078d0f7c08949cf2070d3a863fb3b2c7db73fd2108b1af87ce4d20be1aa16`, fixed DEX epoch 1. Dedicated keys/manifests/synthetic Oracle/relay gas payers were prepared but not applied. The 254 CLX total test-fund budget planned 105 per trader and 6 per relay checkpoint payer, supplied through ordinary transfers. No new genesis allocations or direct live-balance edits were made.

The user had instructed that they be notified when initialization was due. Deletion/initialization had not yet been authorized at this stage. Notification would follow the final build/tests and exact stop/backup/key-preservation/new-generation startup procedure. Q4 LIVE finance and Q5 A/B/C/60-minute operation had not run. The Q0/Q1 paragraphs below record progress at their respective times.

Q0 IPC observations:8 apps online, 8 shell parents/8 cypher children, 0 other PM2 apps. All 8 nodes returned chainId 10101919, genesis `0xe69022ef43f6d238846015e0a4e7cdd190cf45e9fd98cfa0666acb482a05017e`, TX 3502/key 502/peer 7, the same TX hash `0xc21cec94aac027502eff52f7d58ae0bded0941f193c6b7f0b040ac0dd027e73a`, and state root `0x2f0e9e69ed2f965f3db94dc7dedc328cd9d34f0a52f01d630fe5b226beeb330f`. These were observation-time values, neither future constants nor independent finality verification. The 7 committee nodes had mining=true; Common had false and active communication.

The deployed/running cypher hash was identical across 8 nodes: `bcfb21b13a82eb0a9ffdfec843a5d7c3dededf735607da680b8dfa43db2759f8`, recorded as the pre-change baseline. DEX participants/sidecars/relays:0. The current genesis had no dexDevnet configuration. A formal consensus upgrade to add finance to the existing CLX was unimplemented; genesis authentication was not weakened. Ordinary binary updates using the same DB could proceed; live finance awaited new-generation initialization preparation/separate approval and storage implementation.

Q1: An ordinary release build through the actual Makefile was running in dedicated `build/stage/live-q1-release`. Deployed binaries/DBs/PM2 had not yet changed. New launchers explicitly listed 8 apps, used absolute paths and exec, omitted external-IP lookups, bound Common HTTP/WS to loopback, and did not automatically start PoW/DEX. Existing ordinary-CLX miner.start also starts committee FHS, and miner.stop also stops FHS, so no stop was performed to conserve resources.

The storage-cap audit is [live-storage-cap-audit.md](live-storage-cap-audit.md). Generation switching for WAL 128, financial fees 128, anchor 1024, etc. was still NOT_IMPLEMENTED. Simple compaction/deletion of old history was not treated as completion.

Pre-execution assessments remain in [live-acceptance-matrix.md](live-acceptance-matrix.md). Historical isolated-test PASS results are not copied into LIVE results.

## Q1 Execution Record (2026-09-23, Work in Progress)

Release build PASS; genesis/native/CLI focused race across 4 packages/63 PASS events. New binary SHA256 `33f22a4677f35baf7d6e492b46931b8436566a17a2d5fa3f0aed1977ebee768e` was installed atomically at both deployment paths. Eight launch wrappers changed to exec paths; Common HTTP/WS bound 127.0.0.1; committee FHS was restored through existing miner.start/IPC; Common PoW was not started.

Planned stopping of the designated 8 apps removed all 16 old PIDs, with PM2 exit code 0 and 0 SIGKILL records. While stopped, 2001 regular files/12,359,931,946 bytes across 8 datadirs were saved to a private cold backup; all hashes matched and sync was confirmed. No DB/voting-WAL/nonce rollback or initialization occurred. Because PM2 restart does not update pm_exec_path when only the script changes, the same wrappers were temporarily changed atomically to exit 0 stubs and stopped with 60s/treekill/autorestart=false. Pure tests of 16 settings and independent review checked the procedure.

All 8 new executables, PM2PID=cypherPID, same genesis/same DB height 3511, and ordinary peer connectivity were confirmed. The first authentication-restoration attempt ended NOT_READY before IPC creation, with 0 state changes; after all IPCs were observed ready, the retry passed. No parent shells/orphans remained. Ordinary PM2 startup and IPC restoration of existing committee FHS authentication were distinguished.

Ten ordinary signed TXs of 1 atom each, gas limit 0.01 CLX, were sent through Common HTTP admission. All 10 receipts succeeded and roots/receipts matched across 8 nodes. The first smoke-accounting assertion was retained as FAIL: committee 0, the recipient, also received existing static block rewards during the period, omitted from the TX/fee-only expectation. Current `consensus/colossusX/consensus.go:529` issues 100000 CLX to nonempty blocks and other qualifying blocks. Also, eth_getBlockByNumber replaces the displayed miner with the keyblock address, so independent reconciliation using canonical RLP for actual header.Coinbase was in progress. This change did not introduce new DEX issuance. New submissions were paused.

Storage-cap generations were only at the [v4 design proposal](live-storage-generation-v4-design.md) stage, unimplemented. Live-finance genesis activation and mid-run initialization had not occurred. Q2–Q5 were incomplete.

Q1 accounting reconciliation: retaining the initial FAIL, an independent Go model over 19 canonical-RLP blocks and historical balances for every recipient were obtained again and passed. Existing static issuance of 1 million CLX, gas 210000000000000 atoms, Common 42000000000000 atoms, burn 168000000000000 atoms, and transfers 10 atoms were reconciled separately. A new 10-TX smoke run after correction also passed, with blocks/receipts matching across 8 nodes. This was an ordinary-CLX deployment regression, not DEX finance. New genesis config 4/storage-generation source changes were still under development and absent from deployed binary 33f22a….
